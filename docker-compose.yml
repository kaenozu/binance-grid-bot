version: "3.8"

services:
  grid-bot:
    build: .
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      - bot-data:/app/data
      - ./logs:/app/logs
    stop_grace_period: 30s

volumes:
  bot-data:
