# Binance Grid Bot - 開発ロードマップ

## 現在の状態

- グリッド取引のコア機能は完成（注文配置・約定検知・リスク管理・PnL計算）
- 単体テスト 102件 全PASS
- バックテストエンジンあり（手数料計算済み）
- README・ADR・ロードマップあり
- SQLite 状態永続化・起動時注文同期・CSV/JSONエクスポート対応
- 動的グリッドシフト対応
- テストログと実運用ログの混合問題を解決済み（pytest 時ファイル出力無効）
- Phase 1〜2 完了済み

## 方針

- CUI 完了を最優先。画面・通知は含まない
- VPS で `python main.py` だけで安定稼働できる状態を目指す
- 各 Phase は前の Phase が完了してから着手

---

## Phase 1: 再起動安全性 ✅

- 1.1 手数料反映 ✅
- 1.2 停止時のポジション決済 ✅
- 1.3 状態永続化（SQLite） ✅
- 1.4 注文同期 ✅

## Phase 2: 運用品質 ✅

- 2.1 Docker 化 ✅
- 2.2 CI + Lint ✅
- 2.3 トレード履歴エクスポート ✅
- 2.4 WebSocket 対応 → `src/ws_client.py` のみ完成。未統合
- 2.5 テスト強化 → 102件。未統合モジュールのテストなし

## Phase 3: 戦略拡張（コア完成、未統合）

- 3.1 動的グリッド → 完成済み（`src/grid_strategy.py`）
- 3.2 マルチペア → `src/multi_bot.py` のみ。未統合
- 3.3 ペーパートレード → `src/paper_client.py` のみ。未統合

## Phase 4: 保守性（部分完了）

- 4.1 API ウェイト管理 → `src/api_weight.py` のみ。未統合
- 4.2 ログローテーション ✅（`RotatingFileHandler`）

## 未完了タスク（低優先度）

- ws_client / multi_bot / paper_client / api_weight → bot.py への統合
- 異常系テスト追加（タイムアウト・レートリミット）
- テストカバレッジ 80% 以上への引き上げ

### 4.3 API ウェイト管理

- Binance API のリクエストウェイトを追跡
- ウェイト残量が少ない場合は自動スリープ

### 4.4 ログローテーション

- `RotatingFileHandler` に変更
- サイズ上限・世代数を設定

---

## 次の推奨タスク

1. **Testnet 長期稼働テスト** — 今の設定（GRID_COUNT=7, INVESTMENT_AMOUNT=100）で Testnet 1週間程度上を動かす
2. **未統合モジュールの bot.py への統合** — ws_client, api_weight, paper_client, multi_bot
3. **異常系テスト追加** — タイムアウト・レートリミット・ネットワーク切断
